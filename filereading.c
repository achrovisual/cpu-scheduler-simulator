#include <stdio.h>
#include <string.h>
#include <stdio.h>

#define MAX_STRING 5000
#define MAX 100

int main(){
    char string[MAX_STRING];
    int params[3];
    int processValues[MAX][3];

    //read through file

    //filter \n,\(space), and \r from string array, store first 3 vals in params, the rest in processValues
    //NOTE: after populating processValues, check the final value of the row Iterator if it is == to params[1] (use while loop)

    //check parameters from param array

    
    return 0;
}